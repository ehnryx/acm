int n;
cin >> n;
vector<int> a(n);
map<int, int> remap;
for(int i=0; i<n; i++) {
  cin >> a[i];
  remap[a[i]];
}
int rid = 0;
for(auto& it : remap) {
  it.second = rid++;
}
for(int v : a) {
  cout << remap[v] << " ";
}
cout << nl;
