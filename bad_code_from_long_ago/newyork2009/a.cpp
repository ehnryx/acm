#include <bits/stdc++.h>
using namespace std;

#define nl '\n'
#define Get int main() { int line;
#define first cin >> 
#define Input int temp; cin >> temp; vector<int> sorted(10); for (int j = 0; j < 10; j++)
#define Line cin >> sorted[j]; sort(sorted.begin(), sorted.end())
#define The return 0;
#define End }

Get first line;
for (int i = 0; i < line; i++) {
  Input Line;
  cout << i+1 << " " << sorted[7] << nl;
}
The End
