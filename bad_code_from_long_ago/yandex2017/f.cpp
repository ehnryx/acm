#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	queue<int> next;
	vector<int> pause;
	vector<vector<int>> tree;
	vector<pair<int,int>> memo;
	int n, a, b;
	cin >> n;
	tree.resize(n+1);
	memo.resize(2*n);
	for (int i = 0; i < n-1; i++) {
		tree[
	}

	return 0;
}
