#include <bits/stdc++.h>
using namespace std;

int main() {
	int x, y, a, b;
	cin >> x >> y >> a >> b;
	cout << fixed << setprecision(10) << ((x-a)*(x-a) + (y-b)*(y-b)) * 0.916297857297023 << endl;
	return 0;
}
