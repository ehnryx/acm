#include <bits/stdc++.h>
using namespace std;

#include "../../lca/data_structure/segment_tree.h"

constexpr char nl = '\n';

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int n, m, k;
  cin >> n >> m >> k;
  vector<char> s;
  for (int i = 1; i <= n; i++) {
    cin >> s[i];
  }

  return 0;
}
